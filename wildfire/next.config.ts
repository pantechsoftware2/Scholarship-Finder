// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // example:
  // reactStrictMode: true,
  // experimental: {
  //   typedRoutes: true,
  // },
};

module.exports = nextConfig;
